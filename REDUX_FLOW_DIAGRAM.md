# Redux Data Flow Diagram

## Architecture Overview

```
┌─────────────────────────────────────────────────────────────┐
│                         App.jsx                              │
│                    (Redux Provider)                          │
│                                                              │
│  ┌────────────────────────────────────────────────────┐    │
│  │                   Redux Store                       │    │
│  │                                                     │    │
│  │   State: {                                         │    │
│  │     calendar: {                                    │    │
│  │       resources: [...],                            │    │
│  │       events: [...]                                │    │
│  │     }                                              │    │
│  │   }                                                │    │
│  └────────────────────────────────────────────────────┘    │
│                           │                                  │
│                           │ useSelector / useDispatch        │
│                           │                                  │
│  ┌────────────────────┬──┴──────────┬───────────────────┐  │
│  │                    │             │                    │  │
│  │  Calendar.jsx      │  EventList  │   Future           │  │
│  │  (Child)           │  (Child)    │   Components       │  │
│  │                    │             │                    │  │
│  └────────────────────┴─────────────┴───────────────────┘  │
└─────────────────────────────────────────────────────────────┘
```

## Event Flow: Drag & Drop

```
User Action (Drag Event)
         │
         ▼
Calendar Component (handleEventDrop)
         │
         ▼
dispatch(updateEvent({...}))
         │
         ▼
Redux Store (calendarSlice reducer)
         │
         ▼
State Updated
         │
         ▼
Components Re-render (via useSelector)
         │
         ├──▶ Calendar Component (shows updated position)
         │
         └──▶ EventList Component (shows updated time)
```

## Component Communication

```
┌──────────────────────────────────────────────────────────┐
│  WITHOUT Redux (Props Drilling - Not Used)               │
│                                                           │
│  App ──props──▶ Calendar ──props──▶ EventList           │
│   │                                                       │
│   └──callback──▶ Calendar ──callback──▶ EventList       │
│                                                           │
│  Problem: Complex prop passing, tight coupling           │
└──────────────────────────────────────────────────────────┘

┌──────────────────────────────────────────────────────────┐
│  WITH Redux (Current Implementation)                      │
│                                                           │
│              ┌─────────────┐                             │
│              │ Redux Store │                             │
│              └──────┬──────┘                             │
│                     │                                     │
│         ┌───────────┼───────────┐                        │
│         │           │           │                         │
│         ▼           ▼           ▼                         │
│      App      Calendar    EventList                      │
│                                                           │
│  Benefit: Independent components, shared state           │
└──────────────────────────────────────────────────────────┘
```

## Redux Action Flow

```
Component
   │
   │ User Interaction
   │
   ▼
Event Handler (e.g., handleEventDrop)
   │
   │ Prepare payload
   │
   ▼
dispatch(action(payload))
   │
   │ Action sent to store
   │
   ▼
Reducer (calendarSlice)
   │
   │ Process action
   │ Update state immutably
   │
   ▼
New State
   │
   │ Notify subscribers
   │
   ▼
useSelector Hook
   │
   │ Detect state change
   │
   ▼
Component Re-renders
   │
   │ Display updated data
   │
   ▼
UI Updated
```

## State Structure

```javascript
{
  calendar: {
    resources: [
      {
        id: 'a',
        title: 'Conference Room A'
      },
      {
        id: 'b',
        title: 'Conference Room B'
      },
      // ... more resources
    ],
    events: [
      {
        id: '1',
        resourceId: 'a',
        title: 'Team Meeting',
        start: '2025-12-24T09:00:00',
        end: '2025-12-24T10:30:00',
        backgroundColor: '#3788d8',
        borderColor: '#2c6bb3'
      },
      // ... more events
    ]
  }
}
```

## Adding New Features

### Example: Add Event Delete Functionality

```
1. Add action to calendarSlice.js
   ┌─────────────────────────────────┐
   │ deleteEvent: (state, action) => │
   │   state.events = state.events   │
   │     .filter(e => e.id !== ...)  │
   └─────────────────────────────────┘
                 │
                 ▼
2. Export action
   ┌─────────────────────────────────┐
   │ export const { deleteEvent }    │
   └─────────────────────────────────┘
                 │
                 ▼
3. Use in component
   ┌─────────────────────────────────┐
   │ import { deleteEvent }          │
   │ dispatch(deleteEvent(eventId))  │
   └─────────────────────────────────┘
                 │
                 ▼
4. Event removed from all components automatically!
```

## Benefits Visualization

```
┌─────────────────────────────────────────────────────────┐
│                    Redux Benefits                        │
├─────────────────────────────────────────────────────────┤
│                                                          │
│  🎯 Single Source of Truth                              │
│     └─ All data in one place                            │
│                                                          │
│  🔄 Predictable State Updates                           │
│     └─ Actions → Reducers → New State                   │
│                                                          │
│  🧩 Component Decoupling                                │
│     └─ Components don't need to know about each other   │
│                                                          │
│  🔍 Easy Debugging                                      │
│     └─ Redux DevTools for time-travel debugging         │
│                                                          │
│  📈 Scalability                                         │
│     └─ Easy to add new features and components          │
│                                                          │
│  🧪 Testability                                         │
│     └─ Pure functions are easy to test                  │
│                                                          │
└─────────────────────────────────────────────────────────┘
```

## Real-World Example: Event Update

```
Step 1: User drags event from 9:00 to 10:00
        ┌──────────────────────────┐
        │  FullCalendar Component  │
        │  Detects drag end        │
        └───────────┬──────────────┘
                    │
Step 2: Event handler called
        ┌───────────▼──────────────┐
        │  handleEventDrop()       │
        │  Extracts new time       │
        └───────────┬──────────────┘
                    │
Step 3: Dispatch action
        ┌───────────▼──────────────┐
        │  dispatch(updateEvent({  │
        │    id: '1',              │
        │    start: '10:00',       │
        │    ...                   │
        │  }))                     │
        └───────────┬──────────────┘
                    │
Step 4: Reducer updates state
        ┌───────────▼──────────────┐
        │  calendarSlice reducer   │
        │  Finds event by ID       │
        │  Updates start time      │
        └───────────┬──────────────┘
                    │
Step 5: Components notified
        ┌───────────▼──────────────┐
        │  All useSelector hooks   │
        │  receive new state       │
        └───────────┬──────────────┘
                    │
        ┌───────────┴──────────────┐
        │                          │
Step 6: Re-render
    ┌───▼────────┐         ┌──────▼──────┐
    │  Calendar  │         │  EventList  │
    │  Shows new │         │  Shows new  │
    │  position  │         │  time       │
    └────────────┘         └─────────────┘
```

---

**This diagram shows how Redux creates a clean, maintainable architecture!**
